---
- hosts: all
  remote_user: ansible

  tasks:

    - name: Check Splunk process running
      shell: "ps -ef | grep splunk | grep -v grep"
      register: result

    - debug:
        var: result.stdout_lines
